geometry_msgs/Twist twist               # 运动速度
                                        # ┌───────────────────────────────────────┐
                                        # │         结构内部各个字段意义说明          │
                                        # ├───────────────┬───────────────────────┤
                                        # │     目标键     │         值描述         │
                                        # ├───────────────┼───────────────────────┤
                                        # │ + linear      │                       │
                                        # │   - x         │ X轴线速度(m/s),默认:0   │
                                        # │   - y         │ Y轴线速度(m/s),默认:0   │
                                        # │   - z         │ z轴角速度(°/s),默认:0   │
                                        # │ + angular     │                       │
                                        # │   - x         │ 保留字                 │
                                        # │   - y         │ 保留字                 │
                                        # │   - z         │ 保留字                 │
                                        # └───────────────┴───────────────────────┘
                                        #
geometry_msgs/Pose  centroid            # 质心
                                        # ┌───────────────────────────────────────┐
                                        # │         结构内部各个字段意义说明          │
                                        # ├───────────────┬───────────────────────┤
                                        # │     目标键     │         值描述         │
                                        # ├───────────────┼───────────────────────┤
                                        # │ + position    │                       │
                                        # │   - x         │ X轴质心偏移量(m),默认值:0│
                                        # │   - y         │ Y轴质心偏移量(m),默认值:0│
                                        # │   - z         │ Z轴质心高度量(m),默认值:0│
                                        # │ + orientation │                       │
                                        # │   - x         │ X轴姿态:R(°),默认值:0   │
                                        # │   - y         │ Y轴姿态:P(°),默认值:0   │
                                        # │   - z         │ Z轴姿态:Y(°),默认值:0   │
                                        # │   - w         │ 保留字                 │
                                        # └───────────────┴───────────────────────┘
                                        #
geometry_msgs/Twist weight              # 6自由度权重
                                        # ┌───────────────────────────────────────┐
                                        # │         结构内部各个字段意义说明          │
                                        # ├───────────────┬───────────────────────┤
                                        # │     目标键     │         值描述         │
                                        # ├───────────────┼───────────────────────┤
                                        # │ + linear      │                       │
                                        # │   - x         │ X轴权重,默认值:1        │
                                        # │   - y         │ Y轴权重,默认值:1        │
                                        # │   - z         │ z轴权重,默认值:1        │
                                        # │ + angular     │                       │
                                        # │   - x         │ X-R轴权重,默认值:1      │
                                        # │   - y         │ Y-P轴权重,默认值:1      │
                                        # │   - z         │ Z-Y轴权重,默认值:1      │
                                        # └───────────────┴───────────────────────┘
                                        #
geometry_msgs/Quaternion right_forefoot # 右前足:落脚点位置
                                        # ┌───────────────────────────────────────┐
                                        # │         结构内部各个字段意义说明          │
                                        # ├───────────────┬───────────────────────┤
                                        # │     目标键     │         值描述         │
                                        # ├───────────────┼───────────────────────┤
                                        # │ - x           │ X轴偏移量(m),默认:0     │
                                        # │ - y           │ Y轴偏移量(m),默认:0     │
                                        # │ - z           │ Z轴偏移量(m),默认:0     │
                                        # │ - w           │ 抬脚高度(m),默认:0      │
                                        # └───────────────┴───────────────────────┘
                                        #
geometry_msgs/Quaternion left_forefoot  # 左前足:落脚点位置
                                        # ┌───────────────────────────────────────┐
                                        # │         结构内部各个字段意义说明          │
                                        # ├───────────────┬───────────────────────┤
                                        # │     目标键     │         值描述         │
                                        # ├───────────────┼───────────────────────┤
                                        # │ - x           │ X轴偏移量(m),默认:0     │
                                        # │ - y           │ Y轴偏移量(m),默认:0     │
                                        # │ - z           │ Z轴偏移量(m),默认:0     │
                                        # │ - w           │ 抬脚高度(m),默认:0      │
                                        # └───────────────┴───────────────────────┘
                                        #
geometry_msgs/Quaternion right_hindfoot # 右后足:落脚点位置
                                        # ┌───────────────────────────────────────┐
                                        # │         结构内部各个字段意义说明          │
                                        # ├───────────────┬───────────────────────┤
                                        # │     目标键     │         值描述         │
                                        # ├───────────────┼───────────────────────┤
                                        # │ - x           │ X轴偏移量(m),默认:0     │
                                        # │ - y           │ Y轴偏移量(m),默认:0     │
                                        # │ - z           │ Z轴偏移量(m),默认:0     │
                                        # │ - w           │ 抬脚高度(m),默认:0      │
                                        # └───────────────┴───────────────────────┘
                                        #
geometry_msgs/Quaternion left_hindfoot  # 左后足:落脚点位置
                                        # ┌───────────────────────────────────────┐
                                        # │         结构内部各个字段意义说明          │
                                        # ├───────────────┬───────────────────────┤
                                        # │     目标键     │         值描述         │
                                        # ├───────────────┼───────────────────────┤
                                        # │ - x           │ X轴偏移量(m),默认:0     │
                                        # │ - y           │ Y轴偏移量(m),默认:0     │
                                        # │ - z           │ Z轴偏移量(m),默认:0     │
                                        # │ - w           │ 抬脚高度(m),默认:0      │
                                        # └───────────────┴───────────────────────┘
                                        #
float32 friction_coefficient            # 摩擦系数[0,1],默认值:0.8
float32 landing_gain                    # 落地系数[0,1],默认值:1.0
bool use_mpc_track                      # 是否使用 MPC 轨迹
int64 duration                          # 持续时间(毫秒),默认:1000
