geometry_msgs/Twist twist           # 运动速度
                                    # ┌──────────┬───────────────────────┐
                                    # │  目标键   │         值描述         │
                                    # ├──────────┼───────────────────────┤
                                    # │ + linear │                       │
                                    # │   - x    │ X轴线速度(m/s),默认:0   │
                                    # │   - y    │ Y轴线速度(m/s),默认:0   │
                                    # │   - z    │ z轴角速度(°/s),默认:0   │
                                    # │ + angular│                       │
                                    # │   - x    │ X轴姿态:R(°),默认值:0   │
                                    # │   - y    │ Y轴姿态:P(°),默认值:0   │
                                    # │   - z    │ Z轴姿态:Y(°),默认值:0   │
                                    # └──────────┴───────────────────────┘
geometry_msgs/Point centroid_height # 质心高度(米)
                                    # ┌──────────┬───────────────────────┐
                                    # │  目标键   │         值描述         │
                                    # ├──────────┼───────────────────────┤
                                    # │ - x      │ X轴质心偏移量(m),默认值:0│
                                    # │ - y      │ Y轴质心偏移量(m),默认值:0│
                                    # │ - z      │ Z轴质心高度量(m),默认值:0│
                                    # └──────────┴───────────────────────┘
geometry_msgs/Point right_forefoot  # 右前足:落脚点位置
                                    # ┌──────────┬───────────────────────┐
                                    # │  目标键   │         值描述         │
                                    # ├──────────┼───────────────────────┤
                                    # │ - x      │ X轴偏移量(m),默认:0     │
                                    # │ - y      │ Y轴偏移量(m),默认:0     │
                                    # │ - z      │ 保留字                 │
                                    # └──────────┴───────────────────────┘
geometry_msgs/Point left_forefoot   # 左前足:落脚点位置
                                    # ┌──────────┬───────────────────────┐
                                    # │  目标键   │         值描述         │
                                    # ├──────────┼───────────────────────┤
                                    # │ - x      │ X轴偏移量(m),默认:0     │
                                    # │ - y      │ Y轴偏移量(m),默认:0     │
                                    # │ - z      │ 保留字                 │
                                    # └──────────┴───────────────────────┘
geometry_msgs/Point right_hindfoot  # 右后足:落脚点位置
                                    # ┌──────────┬───────────────────────┐
                                    # │  目标键   │         值描述         │
                                    # ├──────────┼───────────────────────┤
                                    # │ - x      │ X轴偏移量(m),默认:0     │
                                    # │ - y      │ Y轴偏移量(m),默认:0     │
                                    # │ - z      │ 保留字                 │
                                    # └──────────┴───────────────────────┘
geometry_msgs/Point left_hindfoot   # 左后足:落脚点位置
                                    # ┌──────────┬───────────────────────┐
                                    # │  目标键   │         值描述         │
                                    # ├──────────┼───────────────────────┤
                                    # │ - x      │ X轴偏移量(m),默认:0     │
                                    # │ - y      │ Y轴偏移量(m),默认:0     │
                                    # │ - z      │ 保留字                 │
                                    # └──────────┴───────────────────────┘
float32 forefoot_height             # 前足:抬脚高度(米),默认:0
float32 hindfoot_height             # 后足:抬脚高度(米),默认:0
float32 friction_coefficient        # 摩擦系数[0,1],默认值:0.8
int64 duration_ms                   # 持续时间(毫秒),默认:1000
